<form method="post" action="{{ path('app_edificio_delete', {'idEdificio': edificio.idEdificio}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ edificio.idEdificio) }}">
    <button class="btn">Delete</button>
</form>
