{% extends 'base.html.twig' %}

{% block title %}Edificio{% endblock %}

{% block body %}
    <h1>Edificio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdEdificio</th>
                <td>{{ edificio.idEdificio }}</td>
            </tr>
            <tr>
                <th>Condicion</th>
                <td>{{ edificio.condicion }}</td>
            </tr>
            <tr>
                <th>Costo</th>
                <td>{{ edificio.costo }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ edificio.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_edificio_index') }}">back to list</a>

    <a href="{{ path('app_edificio_edit', {'idEdificio': edificio.idEdificio}) }}">edit</a>

    {{ include('edificio/_delete_form.html.twig') }}
{% endblock %}
